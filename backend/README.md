# My College Backend

Python FastAPI backend for the redesigned S. B. J. S. Rampuria Jain College website.

## Run

```powershell
cd "J:\my college\backend"
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
uvicorn app:app --reload --host 127.0.0.1 --port 5000
```

